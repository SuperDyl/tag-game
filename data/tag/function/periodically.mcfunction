# Schedules the tag to run again. Currently set to 5 seconds, but can be easily changed
# Run by tag:load, then by itself
# Runs all functions in the function tag #tag:periodically

schedule function #tag:periodically 5s replace
